// A library class - Used to determine if the provided library is
// indeed a library or not. 
export default class Library {
  constructor(name, data) {
    this.name = name;
    this.data = data;
  }
}
